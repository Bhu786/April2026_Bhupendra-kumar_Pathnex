version means  : 1.2.3
major minor 

============================

argo cd ==> watches git repo and control plane .

if changes comes  on gits all deploy to control plane its like argo cd doing sync between 
between 

==========================

vm => container => k8s (when tough to handle too amny yaml file)
=> heml(when tough to handle charts ) => argocd

=======================

update vs updrade in heml 

=====================

argo cd : is sync tools which only work wirh k8s 

=====================

argocd: whatever written on git it automatically apply to k8s.

Git me likha: 3 pods run hone chahiye
Kubernetes me galti se ho gaye: 5 pods

=====================

main concepts :

Git me jo likha hai = desired state
Cluster me jo chal raha hai = actual state
Argo CD in dono ko match karwata hai (sync)

===================
Argo CD Architecture:
mainly there is 3 components 

1. API Server : entry point of argocd and take request cli or ui

2. Repository Server : git connection 


3. Application Controller (🔥 Most Important)

👉 Kubernetes ke saath kaam karta hai

Git vs Cluster compare karta hai
Sync karta hai

👉 Role: Actual kaam (brain)

4. Dex (Optional)
Authentication provide karta hai (SSO, LDAP)

👉 Role: Login/security

==========================

5. Installing Argo CD :

Pre-requisites:
• Kubernetes cluster (e.g., Minikube, Kind, EKS).
• kubectl configured.


Install:
bash
kubectl create namespace argocd

kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-
cd/stable/manifests/install.yaml


Access Argo CD UI:
bash
kubectl port-forward svc/argocd-server -n argocd 8080:443
Visit: https://localhost:8080


Get admin password:
bash
kubectl get secret argocd-initial-admin-secret -n argocd -o yaml
============================
===============================
=================================

