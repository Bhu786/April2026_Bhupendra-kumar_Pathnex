Here’s a clean 5–6 line summary you can paste into your Excel:

Learned semantic versioning (major.minor.patch) and its importance in software releases.
Understood Argo CD as a GitOps tool that continuously syncs desired state (Git) with actual state (Kubernetes cluster).
Studied the evolution from VM → Containers → Kubernetes → Helm → Argo CD for managing complex deployments.
Explored Helm concepts including charts and the difference between update vs upgrade.
Learned Argo CD architecture including API Server, Repository Server, and Application Controller (core component).
Practiced installing Argo CD on Kubernetes and accessing UI using port-forward and admin credentials.
